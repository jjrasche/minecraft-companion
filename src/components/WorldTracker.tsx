import type { ReactNode } from 'react'
import { Page } from '@factoredui/react'

export function WorldTracker(): ReactNode {
  return (
    <Page name="world-tracker">
      <div className="panel">
        <h2>World Tracker</h2>
        <p className="sub">Phase 6 — map + markers coming soon.</p>
      </div>
    </Page>
  )
}
